# Q1 - A module with two utility functions

def greet(name: str) -> str:
    """Returns a greeting message for the given name"""
    return "Hello, " + name + "!"

def square(number: int) -> int:
    """Returns the square of a number"""
    return number * number